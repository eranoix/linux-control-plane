<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
            Activity só usada pelo GridThroughputBenchmark (androidTest) para
            ter uma Window real para anexar TerminalCanvas/TerminalSurfaceGrid
            e registrar Window.addOnFrameMetricsAvailableListener. Nunca
            aparece no app real: este módulo não é embarcado em :app.
        -->
        <activity
            android:name=".BenchmarkHostActivity"
            android:exported="false"
            android:theme="@android:style/Theme.NoTitleBar" />
    </application>
</manifest>
